# Ambiente — copiar para .env (nunca commitar .env)
NODE_ENV=development

# API (apps/api)
PORT=3000
API_HOST=0.0.0.0

# Web (apps/web) — dev server
VITE_API_BASE_URL=http://localhost:3000

# PostgreSQL — reservado Prompt 17 (não configurar no bootstrap)
# DATABASE_URL=postgresql://user:password@localhost:5432/cisne_dev
