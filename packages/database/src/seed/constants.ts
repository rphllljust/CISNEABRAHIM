/**
 * Fictional identifiers for development seed only.
 * TLD `.invalid` is reserved (RFC 2606) — never a real mailbox.
 */
export const DEVELOPMENT_SEED_LOGIN = 'dev-operator@cisne-rondonia.invalid';

export const DEVELOPMENT_SEED_MARKER = 'cisne-dev-seed-v1';
